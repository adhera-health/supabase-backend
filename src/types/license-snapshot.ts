/**
 * License snapshot fields frozen on patient_invitations at invite send.
 */

export interface InvitationLicenseSnapshot {
  license_client_id: number;
  license_program_id: number;
  core_api_host: string;
}
